{
  "name": "clan-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "seed": "node scripts/build-seed.mjs",
    "check": "node scripts/check-imports.mjs",
    "smoke": "node scripts/smoke.mjs",
    "ocr-test": "node scripts/ocr-test.mjs"
  },
  "comment": "deps are DEV/TEST only — the deployed static site loads tesseract.js from CDN and needs no npm install",
  "devDependencies": {
    "jsdom": "^29.1.1",
    "puppeteer-core": "^25.2.1",
    "sharp": "^0.35.2",
    "tesseract.js": "^7.0.0"
  }
}
